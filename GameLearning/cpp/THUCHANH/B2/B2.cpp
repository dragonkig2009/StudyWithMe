#include <bits/stdc++.h>
using namespace std;

int main() {
    ifstream fi ("./B2.inp"); 
    vector<int> arr;
    int target;
    int x;
    fi >> target;
    while(fi >> x){
      arr.push_back(x);
    }
    for(int i = 0; i < arr.size(); i++){
      int total = target - arr[i];
      if(){

      }
    }
    return 0;
}